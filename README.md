# AtomizeNews_swe_exam
An offline-first verification engine that uses a local LLM to extract atomic claims from news articles  and BLite's HNSW vector search to cross-reference them against primary sources.
